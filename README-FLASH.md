# دليل التشغيل السريع - نظام الامتحانات الآمن

تم استكمال النظام وهو جاهز للعرض التقديمي.

## 1. المتطلبات
- Docker Desktop مثبت ومشغل.
- اتصال شبكة محلية (Wi-Fi) لربط حاسوب الأستاذ والطلاب.

## 2. التشغيل السريع (Docker)
افتح سطر الأوامر (Terminal) في مجلد المشروع ونفذ:

```bash
docker-compose up --build
```

انتظر حتى تظهر رسالة `Serveur Express + Socket.io sur http://localhost:3001` ورسالة `Ready` من الـ Frontend.

## 3. الوصول للنظام

### للأستاذ (على جهاز السيرفر):
1. افتح المتصفح على: `http://localhost:3000/professor/dashboard`
2. **تسجيل الدخول**:
   - البريد: `admin@exam.com`
   - كلمة المرور: `admin` (أو قم بإنشاء مستخدم يدويًا في قاعدة البيانات إذا لزم الأمر، أو استخدم التسجيل إذا توفر).
   *ملاحظة: إذا لم يوجد مستخدم، يمكنك الدخول مباشرة إذا لم يتم تفعيل الحماية الصارمة، أو إدراج مستخدم في جدول users.*

### للطالب (من جهاز آخر في الشبكة):
1. احصل على عنوان IP جهاز الأستاذ (مثلاً: `192.168.1.15`).
2. افتح المتصفح على: `http://192.168.1.15:3000/student/login`
3. **تسجيل الدخول**:
   - أدخل `رقم القيد` (Matricule) أو `البريد الإلكتروني`.
   - كلمة المرور.

## 4. سيناريو العرض (Demo Scenario)

1. **الأستاذ**: ينشئ امتحان جديد (العنوان، المدة). يضغط "Démarrer".
2. **الأستاذ**: يضغط "Surveiller" للدخول لغرفة المراقبة.
3. **الطلاب**: يسجلون الدخول، يرون الامتحان، يضغطون "Accéder".
4. **الطلاب**: يدخلون وضع ملء الشاشة (Plein Écran).
5. **اختبار الغش**: من جهاز طالب، حاول فتح تبويب جديد. سيظهر تنبيه (Toast) للطالب وتنبيه أحمر في شاشة الأستاذ.
6. **العمل**: الطالب يرفع ملف إجابة. يظهر فورًا "Accepted" عند الأستاذ.

## 5. ملاحظات تقنية
- النظام يستخدم `SQLite` كقاعدة بيانات (ملف `backend/database.db`).
- الاتصال في الوقت الحقيقي يتم عبر `Socket.io`.
- تم تعديل النظام ليعمل ديناميكيًا على الشبكة المحلية (يستخدم عنوان IP الجهاز تلقائيًا).

بالتوفيق في العرض! 🚀
