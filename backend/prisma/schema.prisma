generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

// Abdi complètera ici les modèles : User, Exam, ExamStudent, Submission, MonitoringLog
